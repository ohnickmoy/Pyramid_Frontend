const init_state = {
    username: '',
    password: ''
}

function loginReducer(prevState = init_state, action){
    switch(action.type){
        default:
            return prevState
    }
}

export default loginReducer